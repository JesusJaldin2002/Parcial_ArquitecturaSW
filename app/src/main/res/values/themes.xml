<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrimerParcial" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Activar el ActionBar -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <!-- Icono de la casita para el botón de 'Home' -->
        <item name="homeAsUpIndicator">@drawable/ic_home</item>
    </style>

    <style name="Theme.PrimerParcial" parent="Base.Theme.PrimerParcial" />
</resources>
